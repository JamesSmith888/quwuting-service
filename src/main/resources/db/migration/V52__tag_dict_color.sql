-- 标签字典展示配色（2026-08-26，标签级配色）。
-- 背景：舞伴详情-编辑资料-标签支持给展示标签配色（管理员在编辑页长按标签弹出
-- 配色控件：场景色板 + 自定义 hex）。颜色为「标签级」——存在标签字典上，同一标签
-- 在所有舞伴处同色（跨舞伴视觉一致，符合「管理员配置标签外观」心智；与
-- qwt_dancers.profile_tags 存的 id 数组无关，历史关联零改动）。
-- color = hex 展示色（如 #E63946）；NULL/空串 = 默认样式（前端不渲染彩色 chip）。
ALTER TABLE qwt_tag_dict
    ADD COLUMN color varchar(20);
