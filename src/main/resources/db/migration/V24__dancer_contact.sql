-- ============================================================================
-- V24: 舞伴联系方式字段（2026-08-14，与"积分解锁"公共模块配套）
--
-- 背景（需求：联系方式可自由设置是否需要积分查看）：
-- 此前 Dancer 实体明确"不采集联系方式"（隐私边界），联系方式从不存在——
-- 需求要求支持联系方式（可设置积分门槛），故新增 contact 列。
-- 隐私策略：
-- 1. contact 属于舞伴资料的一部分，随 UpsertDancerRequest 走既有 PENDING
--    审核流程（管理员可见，审核通过后公开）；
-- 2. 公开后是否直接可见 = 是否有积分门槛（qwt_points_gates,
--    target_type='DANCER_CONTACT'，target_id = qwt_dancers.id）决定：
--    无门槛 → 详情页直接展示；有门槛 → 解锁后展示（未解锁不下发真实值）；
-- 3. 明文存储（与昵称/简介等资料同级别，MVP 阶段不做字段加密）。
-- ============================================================================
ALTER TABLE qwt_dancers ADD COLUMN contact varchar(100);
