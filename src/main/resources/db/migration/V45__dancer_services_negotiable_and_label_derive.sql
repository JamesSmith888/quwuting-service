-- ============================================================================
-- V45: 服务范围三改（2026-08-26）——新增 negotiable「回头客/熟人可谈」+
-- label 语义调整（服务端权威派生，列结构不变）
--
-- 背景（需求：① 删除「服务标签」输入——label 改服务端权威派生：PACKAGE =
-- 子类别名顿号连接+「包时」、DANCE/ONLINE_CHAT = 类别名，仅 OTHER = admin 手动
-- 录入的「服务内容」；② 新增 per-service 开关「回头客/熟人可谈」（默认开，
-- 价格可私下与舞伴协商，与平台无关——详情页服务卡「可谈」行展示）；③ 包时新增
-- 默认：子类别全选 + 计费默认 300 + 预约要求默认「提前 2 小时」）：
--
-- 1. 新增 negotiable 列（boolean NOT NULL DEFAULT true）——存量服务默认可谈，
--    无需数据迁移。
-- 2. label 列结构不变（varchar(20) NOT NULL），仅应用层语义变化：存量自定义
--    label（含 OTHER「户外露营」等）原样保留；此后仅 OTHER 类别允许手动录入，
--    其余类别由应用层按类别派生覆盖。
-- ============================================================================

ALTER TABLE qwt_dancer_services
    ADD COLUMN negotiable boolean NOT NULL DEFAULT true;
