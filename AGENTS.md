# AGENTS.md — quwuting-service（后端）

> ⚠️ **本文件是主题索引（渐进式披露顶层），不是详细文档库。**
> 详细设计一律按主题拆分在 `docs/agents/` 下，本文件只放项目定位、最小事实与索引表。
> **任何 Agent 修改本文件前，必须先读文末「⚠️ 维护规则」**。

## 项目定位

Spring Boot 4.1 + Java 25 + Spring Data JPA 后端服务，为去舞厅小程序提供舞厅信息查询 REST API（黄页数据展示，无交易/支付逻辑）。连接 Supabase Postgres（外部不稳定环境，韧性为第一优先级）。

## 最小事实（新 Agent 必读）

- **构建/运行**：`./mvnw clean compile`；`./mvnw test -Dspring.profiles.active=dev`（连库并验证 SchemaIntegrityChecker）；`./mvnw clean package -DskipTests`；`./mvnw spring-boot:run -Dspring-boot.run.profiles=dev`
- **HTTP 语义**：只允许 GET 和 POST（禁 PUT/PATCH/DELETE）；统一 `ApiResponse<T>`——业务错 = 200+code、未登录 = 401、404 不存在、DB 瞬时 = 503+5003、未预期 = 500+5000；兜底异常禁以 200 伪装
- **客户端上报的枚举字面量一律经 `WireEnums.parse` 宽容解析（2026-09-11）**：trim + 大写后按枚举名匹配，非法返回 null 判该条非法、**禁猜默认值**。根因：客户端本地领域值小写 `'dance'` 曾直接 `Enum.valueOf` ⇒ 每条账目被判非法、**生产库长期 0 行**（写侧全量拒绝是静默故障的最高危形态，客户端状态行反而显示"已同步"）。只修客户端无效——老版本无法强制升级，收敛点必须在服务端。配套：`rejected > 0` 必须 WARN 留痕；响应带 `rejectedIds` 逐条归因；跨仓门禁见 quwuting 仓 `npm run check:protocol`
- **Schema 演进唯一通道 = Flyway 迁移**（`db/migration/V{n}__*.sql`），`ddl-auto=validate`；新 NOT NULL 列唯一通道 = `@ColumnDefault`；枚举类列禁 CHECK（扩枚举免迁移）
- **连接池韧性（Supabase 不稳定）**：6543 JDBC URL 必含 `prepareThreshold=0&connectTimeout=5&socketTimeout=8&tcpKeepAlive=true`；连接池参数唯一事实源 = `application.yaml`（禁环境 yaml 重复声明）
- **性能第一约束 = 最少 DB 往返**（跨洲 371ms/次）：列表/详情公共数据必须缓存、个人态永不缓存、写路径显式失效——详见 [`29-performance.md`](docs/agents/29-performance.md)
- **舞伴统计**：联系方式解锁只计首次成功获取；每次成功提交邀约均计需求热度。需求明细仅资料管理者/管理员可读，展示资料批量查询且不返回联系方式/openId——详见 [`30-dancer-statistics-integrity.md`](docs/agents/30-dancer-statistics-integrity.md)
- **管理端统计口径唯一事实源 = `UserBehaviorEvent` 目录 + `UserStatsSql`（2026-09-15 根因修复 + 扩展）**：**行为事件目录**（表 / 时间列 / 日列 / 口径档 ACTIVE·COLLAB·SIGNAL·PASSIVE / 分类 / 中文标签 / 关联对象 / 明细字典）是「行为事实」的唯一声明处，`UserStatsSql`（日级事实集）与 `UserBehaviorSql`（事件级事实集：轨迹 `EVENT_DETAIL_UNION` / 全档 `EVENT_FACT_UNION` / 仅主动 `ACTIVE_EVENT_FACT_UNION`）都是它的**生成物**——SQL 字面量受门禁逐字校验（`@Query` 要求编译期常量，故只能字面量承载），**加行为表只改目录**；真实用户谓词（剔 ADMIN 运营号 / `test_` 开发号 / 微信审核账号）与「有效活跃」事实集（12 表**用户主动行为**）同为编译期常量，所有统计仓库/分母只能引用不得抄写；**「活跃」专指主动行为，登录自动打卡只是「打开」**（用户列表的 `lastActiveAt`/`LAST_ACTIVE_DESC` 语义 = 「最近露面」，不得当活跃用）；门禁 `./mvnw -Dtest='UserStatsSqlMirrorTest,UserBehaviorCatalogMirrorTest' test`；细节见 [`35-dashboard-stats.md`](docs/agents/35-dashboard-stats.md)
- **技术债**：~~生产跑 dev profile~~ 已于 2026-08-30 切 prod + RDS MySQL；**敏感配置一律 gitignored yml（2026-08-31 起弃环境变量）**：本地 application-dev/mysql.yaml、生产服务器 `${APP_DIR}/config/application-prod.yaml`（外部化配置），详见 [`14-deployment-and-schema.md`](docs/agents/14-deployment-and-schema.md)
- 命名：表 `qwt_` 前缀；根包 `org.quwuting.quwutingservice`

## 主题索引（渐进式披露）

> 需要细节时按主题查阅对应文档；**禁止把细节写回本文件**。

| 主题文档 | 内容 | 何时查阅 |
|---------|------|---------|
| [`01-build-and-run.md`](docs/agents/01-build-and-run.md) | 编译/测试/启动命令、开发测试数据（seed-dev.sql） | 构建、跑测试 |
| [`02-package-structure.md`](docs/agents/02-package-structure.md) | 包结构总览（按功能分包、controller/service/mapper/repository 分层） | 找类、新模块落地 |
| [`03-auth-and-user.md`](docs/agents/03-auth-and-user.md) | 登录流程、软鉴权、微信 API 调用规范、角色、用户资料与用户态刷新 | 鉴权/用户相关 |
| [`04-venue-domain.md`](docs/agents/04-venue-domain.md) | 场所数据模型（核心信息/地址/坐标/营业时间/消费信息）、门店认领与管理权限、场所动态、**门店图片同步（高德直链 + 工作台纠错生命周期，2026-08-22）** | venue 域改动 |
| [`05-venue-heat.md`](docs/agents/05-venue-heat.md) | 热度公式与权重、统计口径（2026-09-08 三处镜像统一实时含今日：列表排序/热门标记 SQL 锚点 +1 day 对齐热度页 now 上界；满意度偏移仍只在热度页）、各趋势口径、营业稳定性、状态可信度、性能优化、**浏览贡献重构（2026-08-27：来源加权列表0.5/搜索1.5/分享2 + 近7天×2 + ln 压缩，马太效应反馈循环修复）**、**收藏/动态口径收敛（2026-09-01：收藏总数/动态总数存量项退出公式，只计近30天新增——旧双列集合包含重复计 25 分）**、**状态可信度二次修正（2026-09-02：OPEN 分支补时间验证门槛——刚翻正/刚建档门店（本次持续≤7天）即使 0 暂停也只判 MEDIUM，修复"长期停业今日翻正却显示稳定营业"零点莎莎实证；无日志老店兜底 HIGH）** | 热度/统计接口 |
| [`06-listing-and-stats.md`](docs/agents/06-listing-and-stats.md) | 复合评分排序、双查询拆分坑位、标签筛选、城市词表、isHot 热门标记、累计浏览量、**零行为权重守卫（2026-08-27 引入；2026-09-07 阈值上修 ≥15 ≈2 次收藏：单点稀疏行为不再悬崖解锁运营权重，HEAT_SCORE 拆 HEAT_BEHAVIOR + CASE 守卫壳）**、**浏览贡献重构（2026-08-27：排序浏览项 = ln(1+来源加权浏览)，VIEW_BEHAVIOR 独立常量三处镜像）**、**关键词检索 v2（2026-09-02：KW_MATCH 六字段 + 门店同步别名 EXISTS；**2026-09-10 门店别名域补漏：`qwt_venue_aliases EXISTS` 第 8 分支**——<b>2026-09-07 落地时只加了 RELEVANCE_KEYS 别名档 2 与 suggest 分支、漏了 KW_MATCH 谓词本体 ⇒ 别名门店搜不到、档 2 成死代码（suggest 前端浮层已下线）</b>；**三处镜像必须同改、KW_MATCH 是唯一结果集决定方**，详见 38 号文档 §4.0；命中后展示解释见 §4.1「命中即解释」）、拆词 AND（逐词行集探测交集 → :filterIds 白名单静态 JPQL）、LIKE 字面转义 ESCAPE；RELEVANCE_KEYS 相关度排序（名称前缀>子串>其余，OPEN 组内前置、**停业店照常展示不过滤**，keyword null 恒等退化零变化）；GET /venues/suggest 联想（前缀>中缀>别名；**2026-09-02 晚前端调用链下线，接口保留勿删——未来热门搜索复用**）；VenueListKey 加 kwPrefixPattern、filterIds 路径绕缓存）** | 列表接口 |
| [`07-feedback-and-reporting.md`](docs/agents/07-feedback-and-reporting.md) | 统一用户上报（类型/状态机/防刷/**原子幂等 upsert，2026-08-20**/激励下发/**状态类类型下线与采纳联动兜底，2026-08-20**/**管理端响应补上报者信息 userId+nickname，2026-08-28**）、场所状态上报（公示期/采纳联动/紧急公告/**处置三分级采纳·保留·移除 + 已处理视图，2026-08-28**/**列表「最新上报」行双信号轮播 2026-09-04：statusLatestText「{时间}·{类型}·舞友上报」与热度文案共用一行前端 4s 轮播、公示口径含 ADOPTED 分支（采纳也置 deleted=true）、StatusReportLatestService 独立微服务规避构造器循环、仅列表场景注入**） | 上报模块 |
| [`08-reaction-and-rating.md`](docs/agents/08-reaction-and-rating.md) | 评分交互（维度/防刷）、Reaction 系统（每日一记模型、**双层字典架构**（2026-08-24：legacy 语义 code + EmojiCatalog 常见表情目录适配（152 项 → 2026-09-03 去噪收敛 134 项）三极性、域内去重、系统文本渲染零图片）、聚合缓存） | 评分/Reaction |
| [`09-dancer-and-points.md`](docs/agents/09-dancer-and-points.md) | 舞伴生态（8 表模型/审核/认可/可见性）、**认可单票换票 + 可配置多选**（2026-08-15：Reaction 风格表情 chip 单票——参与/同票取消/异票原子换票，开关 dancer.recognition.daily.single（V31 默认 true）关闭 = 多选；**崩溃根因修复**：@Modifying 批量删除替代派生删除 + pg_advisory_xact_lock 同键串行化 + afterCommit 缓存失效；RecognizeResponse 扩展 replacedFrom/myTags/tags(四窗口) 绝对快照，详情响应 +myTags；旧 tags 列表兼容；**表情不复用门店反馈字典——2026-08-24 废止**（双域共用 EmojiCatalog 常见表情目录，舞伴仅收 POSITIVE/NEUTRAL、NEGATIVE 不进真人主页；description 补全））、**舞伴收藏**（独立表/幂等接口/收藏列表仅 NORMAL，能力平权）、**舞伴官方认证**（「信息已核验」：V26 审计日志/状态机可回退/编辑触发待复核/撤销必留痕，2026-08-14）、**舞伴统计**（六图趋势/GET stats/V29 浏览埋点/写路径缓存失效，2026-08-14；**2026-08-21 浏览来源新增 VENUE**——门店详情页「同城舞伴」入口进入：`ViewSource` 枚举加 VENUE、趋势 mega-query 加 venue_cnt FILTER、`DancerViewSourceTrendPoint` 加 venue 列（other = 全量减四来源）；门店域自身不产生该来源对其统计无影响；落库链路零改动；**2026-08-26 解锁信息补短视频**——`countDancerUnlockStats` 旧 SQL 缺 DANCER_VIDEO 分支（短视频解锁不入统计），已补视频分支（target_id 经 qwt_dancer_photos kind='VIDEO' 归集）、照片分支同步收窄 kind='PHOTO'；**2026-08-26 解锁记录明细 GET /dancers/{id}/unlocks**——按内容类型返回解锁记录列表（JOIN qwt_users 软删排除 + LEFT JOIN 媒体取 sort_order/duration_seconds + LEFT JOIN 流水取花费 -delta；免费解锁 transaction_id 为 null → 0；解锁时间倒序无分页），`DancerStatsService.unlocks` 实时查询不走统计缓存 + 舞伴存在性/NORMAL 可见性校验（对齐 gifters），`DancerUnlockRecord` DTO（userId/nickname/avatarUrl/targetType/targetLabel/targetDesc/createdAt/cost，targetDesc 后端权威派生：照片 N / 短视频 · m:ss / 联系方式）；**2026-08-29 列表排序 v2 + 统计图「排名热度」**——HOT 主导信号由「近7天认可」换为「近7天联系解锁 ×3」（付费意向主导，认可降平滑项 ×1；根因：免费点赞与成交零相关——懒懒Q 12 票仅 1 次解锁、3 周 53 解锁 0 成交；**排序信号原则**：每个信号须预测用户目标行为，付费意向主导、免费信号平滑）；权重唯一事实源 `DancerHeatWeights`（对齐 VenueHeatWeights 收敛先例）；统计页新增「排名热度」卡（`DancerStatsResponse.heat`，排序口径公开化，formulaText/formulaDetail 后端权威、对齐门店热度页模式）；解锁写路径入列表缓存失效矩阵（`PointsService#invalidateDancerStatsAfterCommit` + invalidateAll）；**2026-08-31 解锁写路径失效矩阵收敛为 `DancerUnlockCacheInvalidator` 单入口**——四条解锁写路径（直连/获批/自动发放/代找替代）统一走 `afterUnlockWrite`（详情族级联 + 列表精失效 + afterCommit）；根因：中转路径曾漏失效列表缓存与直连路径不对称（用户报告「邀约解锁统计图未记录」排查确立），手抄样板漂移收敛为单入口，合约由 DemandRelayServiceTest 锁定）、**城市值一致性契约（2026-08-21 根因修复 V38）**：门店/舞伴城市必须统一标准行政区划名（picker region 输出「南通市」形态）——历史手填「南通」与「南通市」并存导致同城筛选/门店同城舞伴查 0 条；V38 建 `qwt_city_key`（尾部去「市」规范化键，禁 REPLACE 全替换）+ 数据驱动归一存量（映射从 qwt_venues 推导，零硬编码城市名）；`findPublicPage` 城市匹配升级「精确 OR qwt_city_key 相等」、`findPublicCities` 按 qwt_city_key 去重（MAX 优先带市形态，JPQL→nativeQuery）；写路径表单锁死标准形态（dancer-edit city-picker 数据源 /venues/cities），防御层兜底绕过表单的写入、积分系统（账务规则/礼物赠送/合规红线）、**积分解锁公共模块**（门槛/解锁/模糊图；**2026-08-26 联系方式每日首免可配置 V49**——开关 `dancer.contact.daily.free` 默认 false=下线，开启恢复 `hasGatedContactUnlockToday` 首免判定，有门槛一律按门槛扣费，见 16-ops-config）、**创作者收益计划**（激励视频广告/线下结算，2026-08-14） | 舞伴/积分模块 |
| [`10-messaging-and-sharing.md`](docs/agents/10-messaging-and-sharing.md) | 站内信（消息中心；**2026-09-03 热度域两型**：CROWD_CONFIRMED 热度确认（≥3 一致确认，CrowdReportService）+ CROWD_REPORT_LIKED 收到热度点赞（行级点赞首次赞且非自赞，CrowdReportLikeService，去重 = DB affected==1 无标志列）——均 VENUE 深链、同事务幂等，详见前端 docs/agents/27-venue-crowd-report.md「确认后积分 / 行级点赞」）、关注门店营业状态（触发挂点/幂等/**2026-09-01「收藏即关注」耦合：收藏自动关注 + 取消同步取消 + V65 存量回填 + 收藏列表状态角标 statusChanged 批量注入 + read-by-venue 按店批量已读**）、分享追踪 | 通知/分享 |
| [`11-storage.md`](docs/agents/11-storage.md) | 文件存储（前端直传 Supabase、FileCategory、内容校验安全模型） | 上传相关 |
| [`12-api-conventions.md`](docs/agents/12-api-conventions.md) | HTTP API 规范（仅 GET/POST）、统一响应格式、错误码登记表 | 写新接口前 |
| [`13-code-standards.md`](docs/agents/13-code-standards.md) | JPA 实体、多值字段 JSON 列、Repository、DTO、异常处理、请求耗时日志、Jackson 3.x、命名 | 写后端代码前 |
| [`14-deployment-and-schema.md`](docs/agents/14-deployment-and-schema.md) | 配置管理（**敏感值一律 gitignored yml：本地 dev/mysql + 生产外部 config/application-prod.yaml，弃环境变量注入，2026-08-31**）、生产部署、连接池与数据库抖动韧性、Flyway Schema 演进与完整性 | 部署、Schema 变更 |
| [`15-governance.md`](docs/agents/15-governance.md) | 禁止操作、AI 代理常见错误表、验证清单 | 每次修改后、提交前 |
| [`16-ops-config.md`](docs/agents/16-ops-config.md) | 运营配置（feature flag）设施：qwt_ops_config 表、读写接口、管理端入口、键即代码契约 | 新增可配置产品规则时 |
| [`17-group-chats.md`](docs/agents/17-group-chats.md) | 舞友群（V33：微信引流，平台无一键加群 API → 长按识别二维码；scope 三态维度互斥校验；公开分组读 + ADMIN CRUD；qr_code_url 挂 ImageContentValidator；GROUP_QR 存储分类，2026-08-17） | 群聊相关 |
| [`18-venue-photos.md`](docs/agents/18-venue-photos.md) | 门店照片域（V35 `qwt_venue_photos` 独立表；**2026-08-20 深夜收口：仅 ADMIN 上传直发 PUBLIC**——原普通用户 PENDING UGC 通道 + 频控因个人主体无「社交服务」类目被审核驳回而删除；本人视角回显、管理端逐张审核（保留处理存量 PENDING）；读路径批量注入五参重载 + PUBLIC 变化显式缓存失效；updateVenue 忽略 photos 禁全量覆盖） | 门店照片/相册相关 |
| [`28-recruitments.md`](docs/agents/28-recruitments.md) | **门店招工**（2026-08-29，V61 双表 `qwt_recruitments` + `qwt_recruitment_contacts`）：定位=用工信息展示非招聘服务（无投递/报名闭环，个人主体红线）；仅管理员直发；职位受控枚举 + 必挂门店 + 有效期硬过滤 + 风险词发布确认（1010）+ 联系方式免费获取式按需下发幂等留痕（对齐舞伴联系方式纪律）；P0 后端已落地，前端页面待实施 | 动招工相关 |
| [`29-performance.md`](docs/agents/29-performance.md) | **性能优化**（2026-08-30 首页慢根因定位：跨洲 DB 往返 371ms/次 × 列表接口 8~9 次 = 秒级）：缓存分层策略唯一权威——个人态永不进共享缓存 / **用户级缓存（键=userId 短 TTL 30s 不跨用户泄漏，允许）** / 无坐标列表主查询 60s 缓存（`VenueService.venueListCache`，写路径显式失效）/ 角标人数 30s + 最新上报行 30s（只缓存原始行禁缓存相对时间文案）+ 信任权重 60s（`CrowdReportService` 三级缓存）；带坐标查询永不缓存；**2026-08-30 舞伴域：列表缓存精失效（反向索引 dancerId→keys，排序信号写只清该舞伴条目替代全清）+ 收藏列表用户级缓存 30s + getDetail 8 分支 CompletableFuture 并行（专用 4 线程池对齐 Hikari 上限）+ fetchPhotos 照片/视频门槛解锁合并 IN 查询 + 前端 refreshCurrentUser 30s TTL + view 上报本地队列 10s 批量去重串行**；未竟事项=HEAT_SCORE 双算、DB 迁国内、前端分包 | 性能优化/缓存相关 |
| [`30-dancer-statistics-integrity.md`](docs/agents/30-dancer-statistics-integrity.md) | 舞伴解锁统计事实与需求热度下钻：多条实际发放路径经统一失效器收敛；聚合公开、邀约明细受资料管理权限保护且去标识化 | 改解锁/统计/邀约明细时 |
| [`31-resource-access.md`](docs/agents/31-resource-access.md) | 平台全局 ADMIN + 门店/舞伴资源级授权：能力矩阵、有效期、撤销审计、认领迁移与审核安全展示 | 改角色、管理权限、认领或资料维护入口时 |
| [`32-web-auth-and-venue-sync.md`](docs/agents/32-web-auth-and-venue-sync.md) | **Web 管理后台（2026-08-31，独立前端 quwuting-admin-web，独立于小程序生态）**：密码登录（WEB_ADMIN_PASSWORD 环境变量，扫码链路保留待用）+ 门店同步报告（管线上报幂等 upsert、**2026-09-01 快照机制退出：apply 仅状态反转（EXACT/ALIAS 复用 applyBatch，无 snapshotApplied）+ apply-selected 批量 + apply-item 单条（低置信人工放行）+ detail 注入 apply_state.would_reverse + 更新记录 GET reversals（VenueStatusLog changedBy IS NULL）+ 条目匹配依据 match_detail（管线注入，展示「为什么能匹配到」）**；前端决策漏斗 6 视图，「可直接更新」= EXACT/ALIAS 且可反转 + tab 专属批量写库按钮 + 更新记录弹层 + 匹配依据行 + 平台门店对比块（状态=实时批量查询 status-batch，点击看详情）；页面「拉取数据」一键跑管线 = 后端子进程 + 触发者 token 透传 + 状态轮询，替代手动 main.py）+ 手动映射别名（网上门店名→平台门店，管线 --refresh-aliases 拉取）；部署 admin.starseek.online（nginx 静态+反代，弃 CF Tunnel） | 动 web-auth / venue-sync / Web 后台时 |
| [`33-venue-sync-skill.md`](docs/agents/33-venue-sync-skill.md) | **舞讯采集 Skill 数据接口（2026-09-01；2026-09-10 增「白名单置暂停」通道；2026-09-15 高置信全自动；WorkBuddy 技能 quwuting-venue-daily-sync 配套）**：对话式门店维护闭环（Agent 采集舞讯 → 比对 → **五类对比表格**（①可直接更新/开门 ②需核实/冲突·单源·低置信 ③平台未维护 ④参考 ⑤可直接暂停/关门）→ **表① 高置信永远自动写库 + 自动发公告（2026-09-15 用户拍板，不询问、单源日也不降级）**、冲突/单源/低置信人工逐条放行、**新店一律禁止自动建档（09-11 红线 4）**），与 Python 管线互补（管线不建档新店）；新增 `GET /admin/venue-sync/venues/export`（候选门店按量加载：city/status 筛选 + size≤500 + 轻量字段，替代重型 listVenues）+ `POST /admin/venue-sync/venues/batch-create`（批量新增：同城同名归一化幂等 EXISTED、逐条独立事务、statusLog changedBy=null=Agent 来源）；状态写库双向（2026-09-10）：开门方向复用 POST /admin/venue-daily-openings/batch（DailyOpeningService 权威反转），关门方向新增 POST /admin/venue-daily-openings/batch-suspend（白名单口径：`coveredCities` 内未上榜门店 OPEN→SUSPENDED，非 OPEN 静默跳过、城市范围由调用方保证、**不发数据更新公告**；ApplyVenueSuspendBatchRequest/BatchSuspendResult）；多来源确认门（2026-09-09 → 09-10 收紧为「全源一致」）：≥2 源覆盖该城且结论一致才自动写库（全源点名=开门 / 全源未点名=关门），部分点名=源间冲突、单源覆盖一律人工核实；**⚠️ 2026-09-15 起表①（高置信开门，M==S + EXACT/ALIAS）不受本门约束——永远自动写库 + 自动发公告**，本门自此只拦低置信（CONTAINED）与关门方向；**数据源匹配字典（2026-09-01）**：skill 侧 `reference/xianbao360-venue-dict.json` 沉淀错别字/异体字/后缀/粘连/已确认包含映射，比对前优先查阅、新案例回写；**批量更新标识（V8）**：状态日志 `qwt_venue_status_logs.change_source` 列，Agent+Skill 落库注入 AGENT_BATCH（管理后台「更新记录」展示「批量更新」标签），管理端人工=ADMIN | 动 Skill 数据接口 / 批量新增门店 / Agent 写库时 |
| [`34-announcements.md`](docs/agents/34-announcements.md) | **全局公告（2026-09-01 设计定稿，管理面在 Web 后台非小程序内）**：双场景统一一套系统（运营公告 MANUAL + 数据更新公告 SYSTEM，差异仅 source）；V7 双表 `qwt_announcements` + `qwt_announcement_reads`（已读回执，未读数 NOT EXISTS 派生）；用户端 4 接口 + 管理端 8 接口（全 GET/POST，`UserContext.requireAdmin()`）；**已拍板四项**：towxml 渲染（**P0：Skyline 兼容性必真机验证**，降级单页 WebView/自研子集）/ 首页公告条 + 我的页双入口 / 数据更新公告自动+手动双通道（venuesync 写库后触发，同日防重，模板进 ops-config）/ 已读回执表；管理后台先做底部导航栏 + 功能菜单（AppLayout：van-tabbar，**2026-09-11 「更多」由弹层改设置页 MoreView（登记 MODULES 数组）、撤「门店同步」tab**；**⚠️ Vant fixed+placeholder 布局约定（2026-09-01 根因修复）：外部 class/scoped attr 落在 placeholder 占位层而非 tabbar 本体，该层禁写 transform/filter（会劫持 fixed 的 containing block 导致钉底+飘出屏），视觉样式一律 :deep() 命中本体，fixed 限宽居中用 left:0+right:0+margin:auto 禁 translateX hack**）；**M1-M5 全部落地（2026-09-01）**：V7 迁移 + announcement 域 + E2E 验证；管理后台 = AppLayout + 列表/编辑页（bytemd 1.22）；小程序端 = 首页公告条 + 我的页入口 + 列表/详情页（towxml 3.3.1 裁剪版 632K，**P0 Skyline 待真机验证**）；**M4 数据更新钩子已接**（batchCreateVenues created>0 / applyBatch reversals>0 → createDataUpdateAnnouncement，**开关 announcement.data_update.enabled 默认 false，需管理后台置 true 才自动发公告**）；门禁全过（后端 test-compile + 启动无循环依赖 / 管理端 build / 小程序 tsc+check:tokens）；**2026-09-15 触达等级（未读口径收敛，见 doc「触达等级」节）**：新增 `touch_level`（V26，`ALERT` 计入未读 / `SILENT` 不打扰），未读口径从「全部可见公告 − 已读」收敛为「**需触达（ALERT）**且无回执」——根因 = 09-09「公告分开制」+ 09-15「高置信全自动」后公告变日更高频流水，而「每条公告都是待确认事项」的未读模型未随之演进（每日舞讯天天亮徽标、用户被迫逐条点进详情）；列表响应新增 `unread` 派生字段（判据单点 `AnnouncementService#isUnread`，消费方禁再用 `!read`）；新增 `POST /announcements/read-all` 全部已读（用户主动动作，`INSERT IGNORE ... SELECT` 幂等，只覆盖 ALERT、不污染阅读率）；默认档由 `AnnouncementCategory#defaultTouchLevel()` 单点派生（自动化通道不传参也落对档位），V26 回填 `category<>'NOTICE'` → SILENT；管理端编辑页「用户提醒」控件 + 列表「不打扰」标记 | 动公告 / 全局通知 / 数据更新公告 / 未读徽标口径时 |
| [`47-bulletins.md`](docs/agents/47-bulletins.md) | **行业快讯（2026-09-10 定稿并落地；同日二次定稿：信息流 IA + 表态）**：判据 = **是否需要平台为内容真实性背书**（公告强触达+平台背书 / 快讯弱触达+不背书）。**内容边界 = 只描述「服务可得性」**（哪家店 / 哪个时段 开或关），**禁写事件经过与原因**（平台是发布者，失实即名誉权风险；涉单店负面一律不发）。**复用公告表**：V18 加 `city`/`venue_id`/`dedup_key` 三列 + FLASH 生成列唯一索引（`uk_key_qwt_idx_ann_flash_dedup`，仅「未删 + FLASH + 非空」生效）+ `(category, status, publish_at)` 索引；枚举扩 `AnnouncementCategory.FLASH` / `AnnouncementSource.AGENT`（varchar 免迁移）。**公告域契约冻结**：`AnnouncementRepository` 的 `findVisiblePage`/`findPageByFilters` 带 `category` + `excludeCategory` 双参数、`countUnread` 加 `excludeCategory`（**快讯无已读回执，绝不能计入公告未读数**）；`AnnouncementService#rejectFlashCategory` 拒绝公告管理端写 FLASH。**IA 变更（本轮核心）**：列表接口改下发 **content 全文 + reactions**（`BulletinFeedItemResponse` 取代 `BulletinSummaryResponse`），详情收窄为长文深读 / 分享落地；**根因 = 把公告的「列表-详情」IA 当可复用资产套在消费方式完全不同的信息流上**，判据见文档 §1.1（可复用实体/存储/渲染管线，不复用 IA）。**表态域（V19 `qwt_bulletin_reactions`）**：唯一键 `(user_id, bulletin_id)` 是「一人一条恒一个表情」的**唯一不变量载体** ⇒ 换票 = 原地 UPDATE、取消 = 硬删，走单条 `INSERT ... ON DUPLICATE KEY UPDATE`（**无需**门店每日一票那套 `FOR UPDATE` 串行化——用键表达不变量就不用锁去模拟键）；计数不缓存（整页 IN + GROUP BY）。新增 `BulletinLookupService`（可见性规则单一事实源）解掉「表态需可见性、快讯需表态数据」的**循环依赖**（不是 `@Lazy` 补丁）。字典 = `BulletinReactionCode`（**域适配器**，只挑选 `EmojiCatalog` 的通用情绪，不复制条目；零依赖测试 `BulletinReactionCodeTest` 锁目录漂移）。**2026-09-11 字典 10→16→32 枚**（正向 18 / 中性 8 / 负向 6，4×8 满格；前后端 + 测试 + 47 号文档四处同步）。**信息流正序（2026-09-11 用户拍板）**：快讯列表改时间**正序**（新增 `findBulletinFeedPage`，`ORDER BY publishAt ASC, id ASC`，最新一条在底部——聊天式时间轴），`BulletinService#listVisible` 已切换；**公告域 `findVisiblePage` 契约冻结零改动**（倒序专用于公告强触达语义，快讯倒排不得改公共查询）。接口：用户端 2 读 + **1 写（表态 toggle）** + 管理端 7 + **Agent `POST /admin/bulletins/agent-publish`**（一步 create+publish，`dedupKey` 幂等 = 查询前置 + 23505 兜底，**命中不改写字段**）。发布 Skill = `quwuting-bulletin-publish`。**浏览统计（2026-09-11，V20 `qwt_bulletin_views`）**：每条快讯底部展示查看人数，口径 = **信息流展示即计**（列表/详情每成功加载即批量上报 `POST /bulletins/views`），唯一键 `(bulletin_id, user_id, view_date)` 去重（每人每条每天 1 次）；写入 = JdbcTemplate 单语句多行 upsert（一次 DB 往返，禁逐条）；读取 = 整页 IN + GROUP BY，列表/详情下发 `viewCount`，无缓存无热度联动。**五稿字段删除（2026-09-11）**：快讯域**无标题字段**（用户拍板"快讯完全不需要标题"）——三个请求 DTO 删 `title`、三个响应 DTO 把 `title` 换成服务端**派生只读** `excerpt`（`bulletin/BulletinExcerpt` 单点规则：去 markdown 标记取前 30 字，写入/下发共用）；**共享表 `qwt_announcements.title` 列保留**（与公告同表且 nullable=false，公告域必需）⇒ 快讯写入写派生摘要作兼容位，**读取侧一律从 content 现算**（历史行旧标题自然不可见，零迁移）；老调用方带 `title` 被忽略（不 400）。 | 动快讯 / 行业情报发布 / 信息流 IA / 表态 / 公告与快讯边界时 |
| [`35-dashboard-stats.md`](docs/agents/35-dashboard-stats.md) | **运营数据看板（2026-09-06 P0，admin-web Dashboard + `GET /admin/users/daily-stats`）**：真实盘子口径三线趋势（注册=登录建号剔 ADMIN/test 号 / 打开=登录自动打卡 / 真实互动=当日 12 行为表去重）+ **打卡型噪音识别**（注册后从未真实互动，9-3 起占比 60%+ 疑似微信审核/巡检）；**MySQL 8 方言 mega-query**（UserDailyStatsRepository，WITH RECURSIVE 补零 + 噪音 NOT EXISTS；PG 环境勿执行）；admin-web 新增 Dashboard 页（echarts 5.5 按需注册，登录默认落地页）；噪音占比柱 ≥60% 红提示对照提审日期；**微信审核账号标记（2026-09-09 V17）**：`qwt_users.wechat_review` 存量名单预标记（TO/last night's stars/上报>2 审核号）+ `POST /admin/users/{id}/wechat-review` 手动增减 + 统计口径三处排除（统计条/大盘四序列/公告触达分母）——统计去噪非处罚，账号不删；P1/P2=漏斗/游客转化/分享归因/留存 cohort，admin-web 用户管理页待接；**计时·账本使用面板（2026-09-14）**：`GET /admin/spend/usage-stats`（SpendStatsRepository 独立只读统计仓，单数据源 = qwt_spend_entries，计时器云端痕迹唯一 = 结算自动入账 source=DANCE，禁第二套上报通道）+ admin-web `SpendUsagePanel` 挂 Dashboard 独立加载（记账用户/近7日活跃/计时场次/计时用户 + 堆叠趋势 + 支出分类 + 门店 TOP，同口径剔 ADMIN/test_/wechat_review）；**用户下钻（09-14 二轮）**：`GET /admin/spend/usage-users`（记账用户列表，SpendUsersView）+ `GET /admin/spend/users/{id}/entries`（用户详情「计时·账本」卡，只回未软删；行点击复用 user-detail 路由）；**口径单一事实源（2026-09-15 根因修复）**：`UserStatsSql` 编译期常量（真实用户谓词 + 有效活跃 12 表事实集，事实 = 动作发生而非当前有效）取代 3 仓散落抄写；「活跃」= 主动行为，**不得含登录自动打卡**（旧顶卡「四源 MAX」口径已修）、用户列表的「最近露面」另立名不与「活跃」混用；门禁 `UserStatsSqlMirrorTest`（9 条：成员完整 / 打卡禁入 / 消费方必须引用常量 / 自证式互补）；**用户留存分析（2026-09-15）**：`GET /admin/users/retention`（活跃趋势 老用户vs新增 / 留存曲线 / 批次矩阵 D1-D30，**未到期 = null 非 0%**，比率前端派生，附**口径自证**：全部账号 = 有效用户 + 运营开发号 + 微信审核号）+ admin-web `/retention` 页；⚠️ 全局 `default-property-inclusion: non_null` 会**删掉 null 字段** ⇒ 带「无此观测」语义的字段必须 `@JsonInclude(ALWAYS)`，否则前端读到 undefined 会算出 NaN（2026-09-15 矩阵 NaN% 实例）；**用户行为轨迹与行为分析（2026-09-15，资料协作域扩展）**：行为事件目录 `UserBehaviorEvent`（18 事件 = 主动 12 / 协作 1 / 信号 1 / 被动 4，口径档即「算不算活跃」的唯一裁决）+ 事件级事实集 `UserBehaviorSql`（轨迹 / 全档 / 仅主动三条，SQL 字面量由门禁逐字校验目录生成结果），三端点 `GET /admin/users/behavior-analysis`（平台：类型分布 / **活跃分层按「活跃天数 ÷ 可用天数」归一**（与「未到期 ≠ 0%」同族，避免把新注册判成沉默）/ 行为宽度 / 活跃时段 24 格 / 口径自证）+ `GET /admin/users/{id}/behavior-timeline`（18 事件源合并时间线，含打开与被动事件**但各带口径档标签**，禁带敏感列）+ `GET /admin/users/{id}/behavior-profile`（活跃天数 vs 打开天数并排）；`BehaviorRefNameResolver` 为关联对象名与明细字典的**唯一实现**（旧 `AdminUserStatsDetailService` 私有副本已删并委托）；门禁 `UserBehaviorCatalogMirrorTest`（8 条）；admin-web `/users/behavior-analysis` 页（**路由必须声明在 `users/:id` 之前**）+ 用户详情「行为统计 / 行为轨迹」两卡 + 资料协作页入口 | 动运营统计口径 / Dashboard / 留存分析 / 审核流量分析 / **用户行为轨迹与分析**时 |
| [`36-wxacode.md`](docs/agents/36-wxacode.md) | **分享小程序码域（2026-09-07，前端权威文档 = quwuting 仓 40-wxacode-share.md）**：`GET /venues/{id}/wxacode.jpg`（scene=`id=<venueId>`+登录 UserContext 注入 `&s=<uid>` 归因）+ `GET /wxacode/home.jpg`（scene=`home`）；公开软鉴权 image/jpeg 直出 + Cache-Control 24h（非 ApiResponse JSON）；复用 auth WechatService.getUnlimitedQrCode（access_token 缓存单例，禁第三实例）；`wechat.qrcode-env`（默认 release/本地 develop，对齐 web-auth 先例）；`page\|scene` 24h 内存缓存 + 500 容量护栏；零 DB 迁移；`/wxacode` 根路径被舞伴码占用已避开；与 dancershare WxacodeService 同构独立（舞伴域休眠不合并） | 动分享码/小程序码生成/归因 scene 时 |
| [`37-wx-subscribe-notify.md`](docs/agents/37-wx-subscribe-notify.md) | **微信订阅消息（2026-09-07，V11，前端权威文档 = quwuting 仓 41-wx-subscribe-notify.md）**：「开门状态变更提醒」一次性订阅（`wechat.subscribe.status-template-id`），营业状态变更服务通知被动触达（站内信互补）；额度模型=用户×模板池（微信不区分门店，收藏时机授权 +「总是保持」静默滚存）；V11 双表 quota（原子 upsert/扣减/43101 清零）+ logs 留痕；授权上报 `POST /user/wx-subscribe-grants`；发送=notifyStatusChanged 同点发 `VenueStatusChangedEvent` → AFTER_COMMIT `WxSubscribeSendService`（全兜底不反噬主流程、join 一次取 openid+额度、access_token 复用 auth 单例禁第三实例、thing≤20 字「门店名·状态」拼接）；SQL 踩坑=8.0.36 行别名歧义回退 VALUES()（SqlTest 真实库抓到） | 动订阅消息/通知触达/额度账本时 |
| [`38-venue-aliases.md`](docs/agents/38-venue-aliases.md) | **门店别名域（2026-09-07，V12，前端展示权威文档 = quwuting 仓 42-venue-aliases.md）**：管理员维护用户可见门店别名（曾用名/俗称）——`qwt_venue_aliases`（生成列部分唯一，软删复活幂等）与 sync alias（管线映射）语义严格分离；搜索 = KW_MATCH 第 8 命中分支（EXISTS，findIdsByKeyword 拆词 AND 自动继承；**2026-09-10 补漏：该分支 2026-09-07 落地时漏写，别名搜索曾完全失效，见 §4.0**）+ RELEVANCE_KEYS 别名档 2（名称前缀>子串>**别名**>其余，keyword null 恒等退化零变化）+ suggest 中缀兜底；详情 = VenueDetailResponse.aliases（公共缓存体，冷启动 +1 查、写路径 invalidateDetailPublic 失效）；管理端 = /admin/venue-aliases 四接口 + admin-web「门店别名」视图（venue-search 候选 keyword 空兜底最近收录、不限状态）；**2026-09-10「命中即解释」→ 2026-09-16 通用化（§4.1，用户拍板）**：列表 `VenueResponse.matchedHint`（`VenueMatchField` 四值 + 原文；单值/条件下发，优先级 ALIAS > SYNC_ALIAS > ADDRESS > DESCRIPTION，**只收「卡片上不可见」的载体**——name/city/district/tags 由前端对既有行染色自证、禁止进解释行）+ `VenueService#loadMatchHints` 当页一次 IN 两张别名表内存字面判定（只补展示不改结果集、与 KW_MATCH 同字面口径、大小写不敏感；**用原始词定位**——转义改变串长会让简介切片失真）+ 地址脱敏连带 `VenueResponseMapper#demaskMatchHint`（城市级地址命中时 text 降 null，前端渲染「需联系获取」）⇒ 卡片名称正下方条件渲染「{载体} · {原文}」；**原 matchedAlias 由 matchedHint(ALIAS) 取代、无兼容双通道；地址可见性判定唯一归 Mapper；禁替换主标题、列表禁下发 aliases 数组**；**旧禁令「禁回退 sync alias sourceName」同日修订**——实证 323 条 source_name 无渠道后缀、城市前缀仅 2 条且为店名本体，改为 `sanitizeSyncName` 保守裁剪后作为 SYNC_ALIAS 展示（剥前缀方案被实证否决）**门禁 = `VenueAliasMatchMirrorTest`（别名载体三处镜像存在性断言，零依赖）** | 动门店别名/搜索命中口径/详情下发时 |
| [`39-venue-snapshot.md`](docs/agents/39-venue-snapshot.md) | **门店离线快照（2026-09-08，前端消费权威文档 = quwuting 仓 36-offline-resilience.md）**：`GET /venues/snapshot?since=`（公开读）时间戳游标增量同步——since 空 = 全量（~1000 行/~1MB），非空 = updatedAt≥since 活跃行 + 软删 id 剔除集；**游标 vs 全局版本号根因** = 版本号要求全写路径记得 bump 漏即漂移，游标靠 BaseEntity.updatedAt 自动维护零侵入；VenueSnapshotItem = VenueResponse 静态子集（动态信号与 photos 刻意不含——强时效/远程资源离线无意义）；since 非法按全量容错（幂等资源宁多勿缺）；转换复用 VenueResponseMapper.toSnapshotItem | 动离线快照协议/门店静态数据批量下发时 |
| [`40-spend-ledger.md`](docs/agents/40-spend-ledger.md) | **消费账本服务端（2026-09-11，V16 `qwt_spend_entries` + V21 `direction` 收支方向）**：三接口（sync 批量幂等上报 / overview 月度聚合一次往返 / entries 游标增量含软删行）；枚举协议字面量经 `WireEnums.parse` 宽容解析（**小写 'dance' 曾被全判非法 ⇒ 账目从未上云 ⇒ 前端"0.1 秒归零"**）、`rejectedIds` 逐条归因、rejected 必 WARN 留痕；**direction 缺省 EXPENSE（老客户端兼容），overview 聚合只取 EXPENSE（统计页纯支出口径），场次仍含收入 DANCE（44 号 §24）**；域权威文档 = quwuting 仓 44 号 §21/§24 | 改账本同步/聚合接口、排查账目不上云时 |
| [`49-venue-activities.md`](docs/agents/49-venue-activities.md) | **门店营业活动域（2026-09-15，V27 建 `qwt_venue_activities` + `qwt_venue_activity_checkins`）**：活动里**只有三样必须结构化——调度 / 权益类别 / 核销方式**，其余全是自由文本（⛔ 禁做活动模板 DSL，`qw-demo` 是前车之鉴）。外层有效期走**策略模式**（`ActivityScheduleStrategy` + `ActivityScheduleStrategies` 构造期完整性自检：漏实现枚举值直接启动失败）；内层生效窗口走纯数据。**跨夜**：`close < open` 即结束于次日凌晨，`ActivityWindow.contains/endFor` 单点收口（naive 比较会让跨夜窗口永不命中）。**派生权威** = `ActivityStateResolver`（stateKey + `nextChangeAt` 绝对时刻）。到期 **30s 调度强转 OFFLINE**，查询只认 `status`、**不做时效过滤**。打卡幂等靠唯一键 `(activityId, userId, activityDate)`，`activityDate` 服务端填写，**不入热度公式**。错误码 1033/1034。**传播归因（V28）**：`qwt_venue_shares` 加可空 `activity_id`（存量不回填、非外键），管理端列表出 **传播 → 打开 → 到店** 漏斗（`shareCount`/`openCount`/`checkinCountTotal`，四个计数一批 `GROUP BY` 取）；⚠️ 分享/打开数受 60s 频控 + 小程序无"分享成功"回调影响，是**趋势量级不是承诺值**。**发布通道（2026-09-17）**：本域**无商家通道**（含优惠承诺、属经营信息），Agent 侧经 `POST /admin/venue-activities/create` + `publish=true` 一步发布；⚠️ **与快讯域的关键差别 = 本接口没有 `dedupKey`**（无幂等保护，重复提交即多条）⇒ 发布前必须先用 `GET /admin/venue-activities?venueId=` 防重。配套 Skill = `quwuting-venue-activity-publish`（与公告/快讯 skill 同在 `scripts/sync-skills.sh` 双副本管理内） |
| [`48-venue-status-authority.md`](docs/agents/48-venue-status-authority.md) | **门店状态权威层级（2026-09-14，V25 `qwt_venues` 加 4 列）**：解决「管理员手工修正的状态隔天被每日舞讯冲掉」——根因是 `status` **没有所有权模型**（5 个写入点全 unconditional last-write-wins，库里无字段记录「这个值是谁的判断」），而舞讯是第三方整理、会漏报/误报。**不变量 = 人工直改 &gt; 外部舞讯推断**，且**下沉为服务端不变量**（判定唯一实现 `VenueStatusGuardService`，不在采集 Skill 侧维护黑名单——Web 后台同步入口 `VenueSyncReportService→applyBatch` 走另一条路，Skill 侧管不到；锁过期语义也无处安放）。字段：`status_source`（MANUAL/SYNC，**表达所有权**，与日志 `change_source` **表达通道**分工不重叠——历史包袱：日志 `ADMIN` 实义是「人工确认了舞讯条目」不等于人工直改）/ `status_locked_until`（人工锁到期）/ `daily_sync_exempt`（永久豁免）/ `sync_note`。门禁：外部两通道写库前先判「已豁免 → 跳过 EXEMPT」「锁未过期 → 跳过 LOCKED」，**判定放在「确需动作」之后**（免污染汇报口径）；`source="ADMIN"`（管理端勾选应用）人工背书**可越锁**但同样清锁交回自动同步；落库必清锁防悬空锁。**锁时长不对称 = 错判代价不对称**：人工置 OPEN 3 天（意图时效极短，锁长了用户白跑）/ 置停业类 7 天；`CLOSED`/`RENOVATING` **天然免疫**（两通道只动 OPEN↔{CEASED,SUSPENDED}）。**反复冲突 → 永久豁免**而非加长锁（问题是结构性而非时间性）；**明确不做**历史回溯（反推会把几个月前一次性修正全锁上，造僵尸状态）与首版迟滞推翻。配置键 `venue.status_lock.{enabled,human_open_days,human_closed_days}`。返回体增 `skippedLocked`/`skippedExempt`/`skipped[]`；`POST /venues/{id}/update` 加可选 `changeSource`（`AGENT_BATCH`=程序化写库改受门禁约束，被拦时**资料照改·状态不动**，堵住 Agent 经人工接口绕过锁的后门） | 动门店状态写入 / 舞讯批量写库 / 人工改状态优先级 / Web 后台门店编辑时 |
| [`50-venue-opening-plan.md`](docs/agents/50-venue-opening-plan.md) | **门店开业计划（2026-09-17，V29 `qwt_venues.expected_open_date`）**：门店状态补上「**将来时**」——`status` 只有 5 个此刻事实取值，而「明天开业」是**计划**，塞进事实栏位则怎么填都别扭（置 OPEN ⇒ 今天就是营业中、用户白跑 + 关注者收「恢复营业」假通知 + 3 天锁窗口与真实开业日错位；报 `RESUMED` ⇒ 语义是"我此刻在场看到它开门了"，且**不采纳也会以「未经核实」形态在列表最新上报行/公告条曝光**）。判据 = **新增状态值是否要全库审计所有 `status` 比较**（要 ⇒ 它该是派生不是状态）：故落成 `date NULL` 列 + 展示层派生 `UPCOMING`（与 `NOT_OPEN_YET` 同构——那是**日内**将来时、这是**跨日**将来时），**热度非营业禁报 / 报告类型守卫 / V25 人工锁与豁免判定全部零改动**。兑现 = `VenueOpeningScheduler`（30s，**独立 Bean**——同类自调用不经代理会让 `@Transactional`/`@Caching` 静默失效）→ `VenueService#applyScheduledOpening`（**第 6 个状态写入点**，与 `reopenByReport` 同构：写日志 `change_source=SCHEDULED` / `changedBy=null`、清空开业日、**打 3 天人工锁** ⟵ 新店开业时舞讯尚未收录、次日未上榜差集必命中，不打锁则「自动开业」第二天自我推翻）；人工提前置 OPEN ⇒ 只清悬挂值（不写日志不通知）；**`null` = 清空**（判据：本字段有合法清空场景且纯新增零存量，与 `status`/`venueType` 的「null = 保留原值」刻意分叉），置 OPEN 即清空（一个计划只兑现一次）；只做开业方向，停业预告留位不做 | 动门店状态写入 / 开业预告 / 门店编辑表单时 |
| [`43-dance-timer.md`](docs/agents/43-dance-timer.md) §48（quwuting 仓，域权威） | **计价规则快照服务端（2026-09-13，V23 `qwt_dance_rule_snapshots` + `dancerule` 包）**：每用户一行（user_id 唯一）整份配置快照（不透明 blob `{version, rules[], selectedRuleId, presetTombstones[]}`），GET 拉回 / POST 幂等整体覆盖（全仓仅 GET/POST）；服务端只校验非空 + UTF-8 ≤32KB（**结构校验归客户端读写两侧**——快照是整体 blob，服务端强 schema 会把客户端演化绑死服务端版本，与 spend 逐条归一化相反）；`updated_at` 兼作同步水位（epoch ms 下发，客户端双方水位都建在服务端时钟）；userId 恒取登录态。修「清缓存后计价规则选择丢失」（规则配置 = 账本后第二块上云的用户数据） | 改规则快照接口 / 排查规则恢复失败时 |

---

## ⚠️ 维护规则（强制：渐进式披露）

本文件曾膨胀至 **2200+ 行**（教训），现重构为索引。**所有 Agent（含未来的 AI 代理）更新本文件时必须遵守：**

1. **本文件只承载索引与轻量描述**：项目定位、最小事实速查、主题索引表。任何详细设计、迭代历史、根因分析、代码示例一律写入对应主题文档（`docs/agents/` 下），本文件只保留「链接 + 一行摘要」。
2. **新增/修改细节 → 先定位主题文档**：按索引表找到对应文档，细节写进文档；同步更新索引表摘要（保持一行）。
3. **新主题 → 新建文档并登记**：无现成文档承载的新主题，在 `docs/agents/` 下新建 `NN-主题.md`（序号延续），复制现有文档头部模板（含维护警告），并在索引表登记一行。
4. **文档膨胀 → 拆分子文档**：单个 `docs/agents/` 文档超过 ~300 行时，拆出独立子主题文档，索引表同步。
5. **禁止把历史演进堆进索引**：每轮迭代的「决策过程/踩坑复盘」只允许进主题文档，且尽量压缩为可复用的规则沉淀，不留流水账。
6. **写完后自查**：本文件应保持在 ~100 行以内；索引表每个主题恰好一行；改动前先读对应主题文档，避免重复/冲突。
